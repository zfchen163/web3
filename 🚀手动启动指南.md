# 🚀 手动启动指南

## ✅ 数据库已就绪

MySQL 容器正在运行，所有表已创建完成！

---

## 📋 启动步骤

### 1️⃣ 打开第一个终端 - 启动后端

```bash
# 进入后端目录
cd /Users/h/practice/web3/chain-vault/backend

# 如果提示 go 命令找不到，先运行：
source ~/.zshrc

# 启动后端服务
go run cmd/api/main.go
```

**预期输出：**
```
Server starting on :8080
Connected to database successfully
Listening for blockchain events...
```

---

### 2️⃣ 打开第二个终端 - 启动前端

```bash
# 进入前端目录
cd /Users/h/practice/web3/chain-vault/frontend

# 如果提示 npm 命令找不到，先运行：
source ~/.zshrc

# 启动前端服务
npm run dev
```

**预期输出：**
```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
```

---

## 🌐 访问应用

打开浏览器访问：**http://localhost:3000**

---

## 🔍 环境检查

如果遇到 "command not found" 错误，请检查：

### 检查 Go 是否安装
```bash
source ~/.zshrc
go version
```

如果没有输出，说明 Go 未安装或未配置到 PATH。

### 检查 Node.js 是否安装
```bash
source ~/.zshrc
node --version
npm --version
```

如果没有输出，说明 Node.js 未安装或未配置到 PATH。

---

## 🐛 常见问题

### 问题 1: `go: command not found`

**解决方案：**

1. 检查 Go 是否安装：
```bash
ls /usr/local/go/bin/go
# 或
ls /opt/homebrew/bin/go
```

2. 如果已安装，添加到 PATH：
```bash
# 编辑 ~/.zshrc
nano ~/.zshrc

# 添加以下行：
export PATH="/usr/local/go/bin:$HOME/go/bin:$PATH"

# 保存后重新加载
source ~/.zshrc
```

3. 如果未安装，安装 Go：
```bash
# 使用 Homebrew
brew install go

# 或从官网下载
# https://go.dev/dl/
```

---

### 问题 2: `npm: command not found`

**解决方案：**

1. 检查 Node.js 是否安装：
```bash
ls ~/.nvm/versions/node/
# 或
which node
```

2. 如果使用 nvm，加载 nvm：
```bash
# 编辑 ~/.zshrc
nano ~/.zshrc

# 添加以下行：
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# 保存后重新加载
source ~/.zshrc
```

3. 如果未安装，安装 Node.js：
```bash
# 使用 Homebrew
brew install node

# 或使用 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.zshrc
nvm install --lts
```

---

### 问题 3: 后端启动失败

**可能原因：**
- MySQL 未运行
- 端口 8080 被占用

**解决方案：**

1. 检查 MySQL：
```bash
docker ps | grep chainvault-db
```

2. 如果未运行，启动 MySQL：
```bash
cd /Users/h/practice/web3/chain-vault/backend
docker-compose up -d
```

3. 检查端口占用：
```bash
lsof -i :8080
```

---

### 问题 4: 前端启动失败

**可能原因：**
- 端口 3000 被占用
- 依赖未安装

**解决方案：**

1. 检查端口占用：
```bash
lsof -i :3000
```

2. 重新安装依赖：
```bash
cd /Users/h/practice/web3/chain-vault/frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

---

## 📊 验证数据库

```bash
# 查看所有表
docker exec chainvault-db mysql -u root -proot chainvault -e "SHOW TABLES;"

# 查看 assets 表结构
docker exec chainvault-db mysql -u root -proot chainvault -e "DESCRIBE assets;"

# 查看现有数据
docker exec chainvault-db mysql -u root -proot chainvault -e "SELECT id, serial_number, brand, status FROM assets;"
```

---

## 🎯 测试功能

1. **连接 MetaMask**
   - 点击页面右上角的 "Connect Wallet"
   - 选择您的账户

2. **注册资产**
   - 填写资产名称
   - 选择品牌（或输入新品牌）
   - 生成或输入序列号
   - 选择生产地
   - 上传图片
   - 点击 "注册资产并上架"

3. **查看资产**
   - 注册成功后，资产会显示在页面上
   - 可以看到图片、名称、序列号等信息

---

## 🔗 相关文档

- `✅数据库迁移完成.md` - 数据库结构详细说明
- `🚀立即启动查看效果.md` - 完整的启动和测试指南

---

## 💡 提示

- 确保在 **两个不同的终端窗口** 中分别运行后端和前端
- 后端必须先启动，前端才能正常工作
- 如果修改了代码，Vite 会自动热重载前端
- 如果修改了后端代码，需要重启后端服务（Ctrl+C 然后重新运行）

---

**祝您使用愉快！** 🎉
