# 🔐 MetaMask 设置指南

## 问题：资金不足

如果 MetaMask 显示"资金不足"错误，说明当前账户在 Hardhat Local 网络上没有 ETH。

## 解决方案：导入 Hardhat 测试账户

Hardhat 本地网络提供了 20 个预配置的测试账户，每个账户都有 **10000 ETH**。

### 方法 1: 使用默认测试账户（推荐）

Hardhat 的默认第一个账户：

**账户地址**: `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266`  
**私钥**: `0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80`

### 导入步骤：

1. **打开 MetaMask**
2. **点击账户图标**（右上角圆形图标）
3. **选择"导入账户"**（Import Account）
4. **选择"私钥"**（Private Key）
5. **粘贴私钥**: `0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80`
6. **点击"导入"**

### 方法 2: 查看 Hardhat 启动日志获取其他账户

Hardhat 启动时会显示所有测试账户。查看启动日志：

```bash
# 查看 Hardhat 日志
tail -200 /tmp/hardhat.log | grep -A 2 "Account #"
```

或者重新启动 Hardhat 节点查看：

```bash
cd /Users/h/practice/chain-vault/contracts
npx hardhat node
```

启动时会显示类似：

```
Account #0: 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266 (10000 ETH)
Private Key: 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

Account #1: 0x70997970C51812dc3A010C7d01b50e0d17dc79C8 (10000 ETH)
Private Key: 0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d

... 更多账户
```

### 方法 3: 使用 Hardhat 默认账户列表

以下是 Hardhat 默认的前 5 个测试账户：

| 账户 # | 地址 | 私钥 | ETH 余额 |
|--------|------|------|----------|
| 0 | `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266` | `0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80` | 10000 ETH |
| 1 | `0x70997970C51812dc3A010C7d01b50e0d17dc79C8` | `0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d` | 10000 ETH |
| 2 | `0x3C44CdDdB6a900fa2b585dd299e03d12FA4293BC` | `0x5de4111afa1a4b94908f83103eb1f1706367c2e68ca870fc3fb9a804cdab365a` | 10000 ETH |
| 3 | `0x90F79bf6EB2c4f870365E785982E1f101E93b906` | `0x7c852118294e51e653712a81e05800f419141751be58f605c371e15141b007a6` | 10000 ETH |
| 4 | `0x15d34AAf54267DB7D7c367839AAf71A00a2C6A65` | `0x47e179ec197488593b187f80a00eb0da91f1b9d0b13f8733639f19c30a34926a` | 10000 ETH |

## 验证账户余额

导入账户后，在 MetaMask 中：
1. 确保切换到 **Hardhat Local** 网络
2. 查看账户余额，应该显示 **10000 ETH**

## 重要提示

⚠️ **这些私钥仅用于本地开发测试，不要在主网上使用！**

- 这些是公开的测试私钥
- 任何人都知道这些私钥
- 在主网上使用会导致资金被盗

## 如果仍然显示资金不足

1. **确认网络**: 确保 MetaMask 连接到 "Hardhat Local" 网络（Chain ID: 31337）
2. **检查账户**: 确认已导入正确的测试账户
3. **刷新页面**: 刷新浏览器页面重新连接
4. **重启 Hardhat**: 如果 Hardhat 节点重启，账户余额会重置

## 快速检查命令

```bash
# 检查账户余额
curl -s http://127.0.0.1:8545 -X POST -H "Content-Type: application/json" \
  --data '{"jsonrpc":"2.0","method":"eth_getBalance","params":["0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266", "latest"],"id":1}' \
  | jq -r '.result' | xargs -I {} node -e "console.log((BigInt('{}') / BigInt(10**18)).toString() + ' ETH')"
```

应该显示: `10000 ETH`

